applications:
 - name: shiny-predix
   memory: 256M
   buildpack: https://github.com/beibeiyang/cf-buildpack-r.git

# services:
# - <your-uaa>

env:
    uaa_service_label: predix-uaa
    # uaaURL: <your-uaa-uri>
    # base64ClientCredential: <your-base64-encoded-client-credential>
    # loginBase64ClientCredential: <your-base64-encoded-login-credential>
